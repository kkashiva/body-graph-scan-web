# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Description

Body Graph Scan Web — a web app that estimates body fat percentage from front and profile body photos.

## Tech Stack (Planned)

- **Framework**: Next.js (based on .gitignore patterns)
- **Language**: TypeScript
- **Deployment**: Vercel

## Current State

This project is in initial scaffolding phase. The repo contains only a README and .gitignore. No source code, package.json, or configuration files exist yet.

When the project is initialized (e.g., via `npx create-next-app`), update this file with:
- Build/dev/test/lint commands from package.json scripts
- Architecture overview (app router vs pages, API routes, key directories)
- Database/ORM and auth setup details
- Image processing approach used for body scan analysis
- Environment variables needed (document in .env.example)
